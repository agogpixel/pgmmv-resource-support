/**
 * Exports release lock callback type.
 *
 * @module pgmmv-resource-support/locks/release-lock.type
 */

/**
 * A function type that represents a way to release a lock.
 */
export type ReleaseLock = () => void;
